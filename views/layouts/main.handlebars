<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>csfvs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link href="/style.css" rel="stylesheet">
</head>
<body>
    <div class="container-fluid p-0">
        <div class="row g-0">
            <div class="border-end sidebar col-3">
                <div class="list-group list-group-flush border-bottom">
                    <a class="list-group-item list-group-item-action {{#if home}}active{{/if}}" href="/">Home</a></li>
                    <div class="list-group-item list-group-item-secondary text-uppercase fw-bold py-2" style="font-size: 0.75rem;">Projects</div>
                    {{#each projects}}
                    <a class="list-group-item list-group-item-action {{#if (isequal @root.project.project_id project_id)}}active{{/if}}" href="/projects/{{project_id}}">
                        <h6 class="mb-0 fw-6">{{name}}</h6>
                    </a>
                    {{/each}}
                </div>
            </div>
            <div id="content" class="col">
                {{{body}}}
            </div>
        </div>
    </div>
</body>
</html>
